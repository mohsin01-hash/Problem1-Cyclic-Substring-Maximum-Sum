In this problem we are calcuting the maximum sum of uniquely occuring characters
we use the value like a=1,b=2,c=3,......,z=26
and calculating the sum by keep track of that each character on a given string only count once there is no repeatation of same character in the count.
